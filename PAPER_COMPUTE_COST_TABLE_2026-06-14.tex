% Compute-cost tables, updated 2026-06-14.
% Requires: \usepackage{booktabs}

\begin{table}[t]
  \centering
  \small
  \caption{Measured LibriSpeech 100h efficiency. GPU-hours are two-NVIDIA H200
  40-epoch training means $\pm$ standard deviation over seeds 43 and 44 and
  exclude decoding and queue time. Peak memory is peak allocated CUDA memory in
  GiB/GPU, reported as means $\pm$ standard deviation over seeds 42, 43, and 44
  from matched two-GPU H100 80GB short profiles with 20 warmup and 200 measured
  batches. Decode time is a matched one-GPU HLG 1-best timing on test-clean plus
  test-other, not a three-seed statistic; VFTA w/o OT uses the same inference
  path as VFTA.}
  \label{tab:libri100_training_cost}
  \setlength{\tabcolsep}{3.5pt}
  \begin{tabular}{lcccc}
    \toprule[1.1pt]
    Method & Params (M) & GPU-h & Peak alloc. (GiB/GPU) & Decode time (s) \\
    \midrule
    Baseline CTC/AED
      & 103.071 & $5.86 \pm 0.06$ & $14.55 \pm 0.04$ & 30.38 \\
    VFTA w/o OT
      & 103.587 & $6.35 \pm 0.26$ & $14.37 \pm 0.03$ & $31.60^\dagger$ \\
    VFTA
      & 103.587 & $10.10 \pm 0.72$ & $14.38 \pm 0.01$ & 31.60 \\
    AdaMER-CTC
      & 103.071 & $14.00 \pm 0.26$ & $14.81 \pm 0.04$ & 30.60 \\
    \bottomrule[1.1pt]
  \end{tabular}
  \vspace{1mm}
  \footnotesize{$^\dagger$VFTA w/o OT was not separately re-timed; it shares
  the VFTA inference graph and acoustic blank-prior head.}
\end{table}
